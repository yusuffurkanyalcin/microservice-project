spring:
  data:
    mongodb:
      database: mongo-rest
      uri: mongodb://localhost:27017
  application:
    name: product-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 0